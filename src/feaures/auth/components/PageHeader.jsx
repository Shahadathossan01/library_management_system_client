import { Box } from '@mui/material';
import React from 'react';
import TextField from '../../../components/ui/TextField';

const PageHeader = () => {
    return (
        <Box>
            <TextField variant='h4' >Registration Now</TextField>
        </Box>
    );
};

export default PageHeader;